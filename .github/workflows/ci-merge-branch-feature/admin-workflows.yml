name: CI Build and merge branch feature/adminadmin

on:
  push:
    branches:
      - main
      - feature/admin
  pull_request:
    branches:
      - main

jobs:
  build-and-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Check for restricted files in .cursor/rules
        run: |
          CHANGED_FILES=$(git diff --name-only origin/main...HEAD)
          if echo "$CHANGED_FILES" | grep -q "^.cursor/rules/"; then
            echo "ERROR: Restricted files found in .cursor/rules/"
            echo "$CHANGED_FILES" | grep "^.cursor/rules/"
            exit 1
          fi

      - name: Build Next.js project
        run: npm run build